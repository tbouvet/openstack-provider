# Declaring Swarm groups
[managers]
[workers]

[managers:children]
meta-Swarm_type_{{ekara_configuration_params.environment.name }}_manager

[workers:children]
{% if ekara_workers | length > 0 %}
meta-Swarm_type_{{ekara_configuration_params.environment.name }}_worker
{% endif %}

[nodes:children]
managers
workers
